# Patch: PDF mặt bằng — dầm 2 nét đen, thép đỏ + vòng STT

Áp dụng lên `ntgiahuy/san` (đã merge `main` @ c36efde):

```bash
cd san
git am < patches/san/pdf-dam-2-net-den.patch
# hoặc copy lib/pdf/generate.ts
```

- Dầm: 2 nét đen theo da (đúng bề rộng B)
- Chỉ thép dùng đỏ; số hiệu = vòng STT + Ødia a
